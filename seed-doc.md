# A(DAI) — Seed Document

**Version:** 4.1
**Date:** April 2026
**Source documents:** Seed Doc v3.1, North Star, Design Brief (Apr 2), Agentic Risk Map, Concept Paper, Living Graph Spec, CR-SQLite Matryoshka Proposal, Team Meetings (31 Mar – 2 Apr 2026)
**Authors:** Iri
**Status:** Active seed document
**Licence:** CC-BY-SA 4.0. The project builds in public.

> This document supersedes v3.1. The four-room Basel model is replaced by a three-layer prototype and a unified experience. The sensing loop tidal rhythms are a hypothesis, not a commitment. The audio layer is post-foundation. What remains unchanged: the architecture, the principles, the gravity model, the merge boundary, the consent architecture, the fork right.
>
> **On the frameworks in this document:** Everything named here is a prototype being explored in public. These are the team's first hypotheses, published so they can be tested, challenged, and revised by the people who use them.

---

## 01. Vision

A(DAI) started with a question: what if the digital arts could sense themselves?

Practitioners carry the real knowledge of their scenes — who influenced whom, what actually happened in a specific studio, which works shifted things. But that knowledge lives in people, not in any shared structure. Scenes develop in parallel, connected ad hoc through systems optimised for synthetic attention. History gets written after the fact by people who weren't in the room. Curators "rediscover" what practitioners never forgot. Collectors acquire work without a clear map of its lineage.

A(DAI) is the infrastructure for that memory. A knowledge commons and semantic graph where practitioner intelligence, lived experience, artworks, and field signals accumulate into something the field can query, traverse, challenge, and build on.

It is *a* canon, not *the* canon. The indefinite article is load-bearing. It produces a reading of the field — structurally honest, provenance-tracked, and contestable. Never the reading.

It is a 10-year infrastructure project. Not a platform. Not a marketplace.

### The core claim

An institution is not a building or a board. An institution is a persistent pattern of intention — something that reliably articulates what the field needs, holds itself accountable to its own stated values, and generates the next question.

The attention economy already has institutions. They are called platforms. They capture looking, redirect it, monetise it. A(DAI) refuses that axis. The system generates intention — directional, purposeful, ethically weighted readings of what the commons needs.

At the centre: the artwork. Not the artist's biography, not the platform's metrics, not the institution's prestige. The work itself — what it does, what it embodies, what it refuses, what it makes possible, who it is in conversation with.

### Why digital arts, plural

"Digital art" sounds like one thing. It isn't. The actual history is a messy, overlapping explosion of code, networks, and machines: algorithmic, cybernetic, networked, generative. Each practice arriving on its own schedule, rarely in sequence.

We use the plural to keep the doors open. What would an institution look like if it were built to move with the practice? What happens when you refuse the idea that a canon has to be complete before it can be read?

### Why now

The NFT cycle demonstrated something specific: the only infrastructure the field had was borrowed from speculation. When the market contracted, the transaction layer contracted with it. But it did not leave nothing behind. Art Blocks, fxhash, Zora, and Manifold survive. On-chain provenance standards persist. Institutional adoption of digital art is a permanent structural change.

The market gave the field provenance of ownership. It never gave it provenance of meaning. A(DAI) builds the missing layer.

---

## 02. Principles

Six instincts guide how A(DAI) is built:

1. **Show the bias.** No one is neutral. A declared position produces stronger knowledge than pretending otherwise.
2. **It matters how you know something.** We track who made a connection, why, from what position, with what evidence.
3. **Contradictions coexist.** When claims conflict, they sit side by side with their provenance intact. The graph remembers how it changed.
4. **The art comes first.** Movements and genealogies emerge from the works. They don't get imposed onto them.
5. **Our vocabulary is a hypothesis.** Every parameter is versioned and open to challenge. Disagreement is evidence.
6. **The system watches for what's forming.** Most knowledge systems reward what's already legible. We're trying to notice when practitioners reach for language that doesn't exist yet — where the field is actually moving. If the system can't see that, it's just an archive with better plumbing.

### Additional principles

7. **Intention over attention.** Never optimise for engagement. Measure relational depth.
8. **Gravity, not hierarchy.** Mass through relations, not rank. Categories form around practice.
9. **Frontier signals are the most valuable signals.** Surface what the system cannot classify. Give it a lifecycle.
10. **Governance at the gate, not inside the data.** The data preserves everything. The merge boundary is where judgment lives. The parameters are open and forkable.
11. **Everything is a prototype until the field says otherwise.** Published for testing. Designed to be rewritten.
12. **Provenance of meaning.** Trace the intellectual arc, not just the ownership chain.
13. **Deep time.** Every decision against a 10-year horizon.
14. **Show the work.** If the reasoning cannot be traced, the system has failed.
15. **Contribution deserves feedback.** When someone gives knowledge, they see what happened with it.
16. **Agent-native.** Agents are first-class actors, not bolt-ons.
17. **Coherence over consensus.** Multiple readings coexist. Disagreement is evidence. The system holds tensions rather than resolving them.
18. **Sympoietic, not autopoietic.** The system makes-with its contributors, its infrastructure, its supply chains.

---

## 03. Strategy

### Mode 1 — the commons is the product

A(DAI) builds the commons. Open, field-owned intelligence under CC-BY-SA 

The success criteria: the more A(DAI)s exist, the better. A community in Lagos or São Paulo or Taipei should be able to fork the data, apply their own merge parameters, define their own vocabulary, run their own canon. The founding team's A(DAI) is one instance among many.

### A canon you can change

You show up, hear the system narrate a perspective on the field, and pause it to push back. Question a connection, propose a different one, offer a first-hand account of something the graph hasn't seen. Contributing feels like conversation, not submission. The provenance layer handles the rigour.

As practitioners show up with testimony and counter-claims, certain parts of the graph get dense with meaning. That density is the field defining itself in real time.

Every canon is partial, generative, provocative. The editorial judgment is field-owned: practitioners shape what the system looks for and how it reads what it finds. Those decisions are part of the commons — challengeable, versioned, forkable.

### Where this goes

We don't want to be the official voice. We want to be the first of many. The narrative comes first. The market follows.

Forks aren't silos. The protocol lets knowledge move between instances. Insights from one can be proposed back to others, keeping the conversation open across the network.

The founding version carries our biases. The system is built to surface them. Everything is CC-BY-SA. We are one instance among many. The rest is up to the field.

---

## 04. The Three-Layer Prototype

The full vision — an immersive audio-first conversational experience with the canon — is the destination. The build is staged in three layers that establish the foundation first. Plants grow from roots, not flowers.

### Why layers, not rooms

v3.1 described four rooms (Sense, Query, Contribute, Connect) — an interaction model for Basel. The rooms assumed a finished system visitors would walk through. But we don't have a finished system. We have a live backend, zero artworks, three edge types, and no editorial guidance docs.

The layers are a build sequence, not an interaction model. Each layer produces something real that the next layer depends on. Layer 1 creates the audience. Layer 2 creates the provocation. Layer 3 captures what only practitioners know. The audio layer sits on top of all of it.

### Layer 1 — Generative landing page + North Star

**What:** Publish the North Star on Substack. Deploy a generative landing page on Netlify. Collect emails.

**Why:** Before anyone can interact with the graph, they need to feel what A(DAI) is. The aesthetic IS the argument. A canon that refuses completeness, a field too vast to see at once, plurality without comprehensiveness — the landing page communicates all of this before a single data point is queried. It also gives us something to send people right now: artists, collectors, institutions. "Here's what we're building. Want to be first?"

**Why first:** It's the fastest to ship and creates the flywheel. People sign up → we have an audience for Layer 2.

**Who:** Piyush (particle/gravitational visualisations, brand system, deploy on Netlify). Iri (North Star article, editorial voice).

**Status:** In progress. Aesthetic direction approved. Brand system under development.

**How the design embodies the principles:**

| Visual choice | Principle it embodies |
|---|---|
| Can't see the whole universe, only a snapshot | A canon that refuses completeness before it can be read |
| Edge diversity as visual weight, not volume | It matters how you know something |
| Generative surface, different every visit | Our vocabulary is a hypothesis |
| Denser nodes where relations cross regions | Gravitational mass through relational density |
| User can customise the aesthetic | Forkability — one of many, make your own |

### Layer 2 — Public data layer + graph

**What:** Automated runs from curated sources populate the graph. Wikipedia for base knowledge. Institutional archives for exhibition histories. Platform APIs for the first artwork nodes. Scout agents with an editorial lens — not valueless scraping but coded values.

**Why:** The graph needs enough substance to be interesting — and provocative. The design principle is provocation: the first runs should produce something substantive enough for people to disagree with. Disagreement drives contribution. Without this layer, there's nothing for practitioners to react to, nothing to correct, nothing to argue with.

**Why second:** It depends on the backend being solid (which it is) and on Iri's source curation. It produces the data that Layer 3 practitioners will respond to.

**Who:** Iri (source curation, editorial framework for agents). Gio (scout agent pipeline, ingestion into CR-SQLite).

**Status:** Starting. Iri identifying 10–15 sources. Gio available from April 9 for the ingestion pipeline.

**Target sources (priority order):**

| Source | What it gives us | Priority |
|---|---|---|
| Wikipedia/Wikidata | Base knowledge, canonical practitioners, structured data | High — start here |
| Institutional archives (ZKM, Ars Electronica, Rhizome) | Exhibition histories, artwork metadata | High |
| Platform APIs (fxhash/Tezos) | First artwork nodes, on-chain provenance | High — fills the 0-artwork gap |
| RSS feeds (art publications) | Dynamic signal flow, current field activity | Medium |
| BlueSky | Practitioner discourse, emerging connections | Medium |
| X/Twitter | Richest practitioner signal | Low — API costs ~$5k/mo |

**Expected output after first 3 runs:** 2,000–5,000+ nodes including first artwork nodes.

**The scout agent pipeline:**
1. Agent receives a source URL + editorial skill file
2. Agent crawls/queries the source
3. Agent extracts entities (practitioners, artworks, concepts, scenes, institutions)
4. Agent classifies edge types (PRACTICES, BELONGS_TO, CREATED_BY, EXHIBITED_AT)
5. Agent writes to `intake_queue` with provenance metadata (`source_origin: ai_generated`)
6. Human reviews queue at `/review` before merge

**Scout agents with editorial lens:** The agents are not valueless scrapers. They carry coded values and a structured reasoning scaffolding — the editorial lens IS the critique of AI built into the practice. The values with which agents perceive are authored by the team and are themselves a contribution to the commons.

### Layer 3 — Intimate layer + practitioner profiles

**What:** Qualitative input from practitioners. The implicit knowledge that isn't accessible on the web — who actually influenced whom, which works shifted things, what happened in that studio. Practitioner profiles with sovereign databases.

**Why:** This is where the unique value lives. The public layer gives you what Wikipedia knows. The intimate layer gives you what practitioners know. The gap between these two is the intelligence. A collector sees exhibition records (public) alongside the artist's own account of why that work exists (intimate). That's the provenance of meaning.

**Why third:** It requires something to react to (Layer 2) and a mechanism for contribution that's been tested. The LLM interview approach is under evaluation — it needs human-in-the-loop or it's a glorified chatbot.

**Who:** JB (artist relations, sourcing archivists and early practitioners). Iri (editorial persona design, human review process).

**Status:** Evaluating. The mechanism for capturing practitioner knowledge at scale is not yet confirmed.

**LLM-assisted interviews (under critical evaluation):** JB proposed LLM-conducted interviews as a scalable alternative to manual recordings (too slow, introduce interviewer bias). The idea: send practitioners a link, LLM conducts the interview with an editorial persona, transcript becomes signal. Vouch system: each participant can invite others they trust.

**Critical caveat:** This needs human-in-the-loop. Without it, it's a glorified chatbot. The intimate layer's value IS lived experience and implicit knowledge — an LLM alone risks flattening exactly what makes practitioner input irreplaceable. The editorial persona must be carefully designed, and human review of extracted signals is non-negotiable. This approach is under evaluation, not confirmed.

**Practitioner profiles:** Each practitioner gets a sovereign database (the Matryoshka's Layer 1). Self-authored data has no merge boundary — if you write to your own DB, free pass. External contributions go through review.

### The audio layer (post-foundation)

**What:** Voice input, TTS narratives, pause-and-challenge, intent classification.

**Why post-foundation:** Audio sits on top of a protocol. Without the protocol, there's nothing to narrate. Without enough graph density, the narrative sounds thin. Without editorial guidance docs, Claude's defaults flatten the vocabulary. Get the data right, get the protocol right, get the graph dense enough — then add the voice.

**What the audio layer includes (when built):**
- Voice input with real-time transcription and language auto-detection
- Intent classification: question, challenge, first-person account, annotation, narrative request
- Pause-and-challenge: visitor pauses narrative, speaks a challenge, hears counter-evidence
- Narrative generation: Claude traverses the graph, generates structured scripts, TTS produces audio
- Every voice interaction generates a signal that enters the merge boundary

**Preconditions:** Data layers solid, protocol working, graph dense enough to narrate meaningfully, editorial guidance docs written.

### Basel

The goal is to have the prototype ready before Basel (June 2026). The deadline is real but the quality bar is higher than "something that works at a fair." We're building a product, not a demo. Basel is where we show it.

Basel is a demo moment, not the scope driver. The MVP ships when it's good, and Basel is the first place to show it if timing aligns.

---

## 05. Concept

### Artists and works at the centre of gravity

The core metaphor is gravitational. An artist's node accumulates mass not from fame or market value but from the density and diversity of their relational connections. More typed edges, backed by higher-confidence and more diverse signals, means more mass. More mass means more gravitational pull.

Artworks are nodes too. A work carries edges to its creator (CREATED_BY), to the techniques it uses (USES_TECHNIQUE), to the concepts it embodies (EMBODIES), to works it responds to (RESPONDS_TO), to institutions where it was shown (EXHIBITED_IN). A work's gravitational mass comes from the same place as a practitioner's: the density and diversity of its connections.

**Edge type diversity per node is the core metric.** A practitioner connected to the demoscene, institutional critique, and generative code through three different edge types holds a structurally significant position — not because they have three connections, but because those connections bridge otherwise separate regions of the graph. A node with 20 PRACTICES edges has less visual weight than a node with 5 edges across 4 different types.

The sensing layer reads artworks for different things than practitioners. Practitioners have trajectories. Artworks have positions: they occupy a point in the field where technique, concept, philosophy, and context intersect. When multiple artworks occupy similar positions, that is a tendency. When an artwork occupies a position no other work shares, that is a frontier. When two artworks RESPOND_TO each other but EMBODY opposing concepts, that is a tension.

Tensions are the most valuable thing the system can surface. Not gaps to be filled. Productive, unresolvable tensions that reveal what the field is actually arguing about.

### Coherence without consensus

The system seeks coherence, not consensus.

**Consensus:** we all agree on what the graph means. Disagreement is a bug.
**Coherence:** the graph's structure is consistent with its own commitments. What it means is deliberately multiple. Disagreement is evidence.

Two contradictory readings are coherent when (a) both have provenance, (b) both are reachable in the graph, (c) neither is deprecated, and (d) the tension between them is a named entity — a TENSION_WITH edge — with its own lifecycle.

### The generative interface

The interface is not a static display of fixed data. It is generative:

- **Different every time.** The entry point shifts. The narrative foregrounds different tensions. The provocations change based on what's been contested recently, where frontier signals are forming.
- **Invites play.** The visitor can ask "what if" questions, request alternative traversals, challenge any claim and hear the evidence. The system is designed to reward curiosity and debate.
- **Worldbuilding through conversation.** When a visitor speaks, they are not just querying — they are contributing to the shape of the canon. Their perspective, their challenge, their first-person knowledge changes what the system knows and how it narrates itself.

### Two data layers

| Layer | What | Source | Confidence | Character |
|---|---|---|---|---|
| **Public** | Crawled from open sources | Wikipedia, institutional archives, platform APIs, RSS, BlueSky | `medium` (agent-classified) | Noisy, substantive, the broad foundation |
| **Intimate** | Qualitative practitioner input | Interviews, direct contribution, recorded conversations | `high` (practitioner-confirmed) | Implicit knowledge, lived experience, the unique value |

Both flow through the same backend. The distinction should be visually represented. The public layer is the Dante's inferno underneath — noisy, maybe wrong, but alive. The intimate layer is the refined, qualitative surface where practitioners share what only they know.

### Provenance of meaning, not ownership

Every node carries full provenance: who contributed the signal, when, how, at what confidence level, whether it comes from lived experience, under what consent.

**Bi-temporal provenance.** Every signal tracks two timestamps: **event time** (when something happened in the field) and **ingestion time** (when A(DAI) observed it). The gap between these two is itself diagnostic — a large gap means the commons is slow to notice.

**Consent architecture.** Consent is not binary. Contributors set machine-readable terms:
- **Use scope:** structural analysis only / full commons
- **Attribution:** anonymous / attributed / attributed with notification
- **Duration:** perpetual / time-limited
- **Revocation:** contributor can withdraw signal; derived edges flagged for review

Consent terms are stored alongside signal provenance and enforced at the merge boundary.

**Third-party representation.** The graph will contain nodes for practitioners who have not directly contributed. Governed: nodes for non-contributors are created as **unconfirmed** with limited metadata. Non-contributors can claim, modify, or request removal of their node. Linking pseudonymous identities without consent is prohibited.

---

## 06. Architecture

### Data layer: CR-SQLite Matryoshka

CR-SQLite extends SQLite with CRDT capabilities — databases that merge without conflicts. The architecture nests:

```
┌──────────────────────────────────────────────────┐
│  FIELD DB (full commons — fat materialised view)  │
│  ┌────────────────────────────────────────────┐   │
│  │  SCENE DB ("Berlin generative art")        │   │
│  │  ┌──────────────────────────────────────┐  │   │
│  │  │  PRACTITIONER DB (artist-rafael)     │  │   │
│  │  └──────────────────────────────────────┘  │   │
│  └────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────┘
```

- **Practitioner DB.** One .db per practitioner. Portable — "give me my data" = hand them a file. Self-authored data merges without boundary.
- **Scene DB.** One .db per scene. Index of practitioners, scene-level signals, materialised aggregates.
- **Field DB.** Single .db for the full commons. Fat materialised view. Gravitational queries run here.

CRDTs guarantee syntactic merge. When a fork re-merges and contradictory edges appear, both persist with full provenance. The merge boundary flags the contradiction. Resolution is human, not mechanical.

**Current state:** Single-DB deployment (4,509 nodes / 17,385 curated edges, two-platform pipeline / 4 live edge types — CLASSIFIED_BY, CREATED_BY, EXHIBITED_AT, EMBODIES — + PRACTICES and 4 others reserved at zero + RESPONDS_TO empty by design / 1 canonical lens + 5 sub-regimes — see CLAUDE.md for the live counts). Per-practitioner split designed, not implemented. Not a blocker for the prototype.

**Decision: single DB first, Matryoshka later.** The full nested architecture is the destination. For the prototype, one database handles everything. The schema is the same either way — the split is a deployment change, not a data model change. Don't build distribution before the data is worth distributing.

### Schema

Three CRR (CRDT-replicated) tables shared across all DBs:

**nodes** — id, type, name, metadata (JSON), created_at, updated_by

**edges** — source_id, target_id, edge_type, signal_id, confidence, charge, event_time, ingestion_time, valid_from, valid_until, invalidated_by, created_at
- Composite PK: (source_id, target_id, edge_type, signal_id)
- Independent observations produce independent rows. "How strong is the connection?" is not a number — it's: "Observed 5 times, by 3 sources, including 1 practitioner self-report."
- Bi-temporal: event_time (when true in the field) + ingestion_time (when A(DAI) observed it)
- Edges are never deleted. When facts change, edges are invalidated with `valid_until` timestamp

**signals** — id, title, source_url, source_type, cla_layer, summary, content, submitted_by, confidence, lived_experience, event_time, ingestion_time, consent_scope, created_at

Plus local tables: `intake_queue` (staging before merge), `settings`.

### Typed edges

**Ship first (reliable classification):**

| Edge type | From → To | What it means |
|---|---|---|
| EMBODIES | Artwork → Concept | The work manifests this concept (621) |
| PRACTICES | Practitioner → Concept | Works with this concept (461) |
| CREATED_BY | Artwork → Practitioner | Made this work (405) |
| EXHIBITED_AT | Practitioner/Artwork → Institution | Exhibition record (300) |
| CLASSIFIED_BY | Any → Classification regime | The lens that positioned this node (283) |
| COLLABORATES_WITH | Practitioner → Practitioner | Active collaboration (183) |
| BELONGS_TO | Practitioner → Scene | Part of this scene (154) |
| USES_TECHNIQUE | Artwork → Concept | Technique or medium used (75) |
| INFLUENCES | Practitioner → Practitioner | Directional influence (4) |

**Reserved (zero rows by design):**

| Edge type | From → To | What it means |
|---|---|---|
| RESPONDS_TO | Artwork → Artwork | Requires evidence of artist intent — practitioner-contributed only, not inferred |

**Deploy when signal volume justifies:**

| Edge type | From → To | What it means |
|---|---|---|
| PIONEERED | Practitioner → Concept | Originated or significantly advanced |
| CRITIQUES | Practitioner → Concept | Actively challenges or works against |
| TENSION_WITH | Concept → Concept | Productive tension |
| EMERGED_FROM | Concept → Scene | Originated in or tied to this scene |
| CONTESTS | Signal → Signal | Explicit disagreement |

Counts reflect the canonical seed (`seed-canon-v1`, April 2026 enrichment). The vocabulary is provisional — what the system can't classify is the agenda for expansion.

### Edge classification rules

- Claude assigns edge types guided by editorial skills files. Judgment belongs in skills, not in Python.
- Agent-classified edges default to `confidence: medium`. Practitioner-confirmed edges get `confidence: high`.
- Correction = just another row. Both rows coexist. Query layer sees the full evidence trail.

### Queries

Field-wide queries (gravitational model) run against `field.db` — single file, single SQL. Drill-down uses ATTACH for specific practitioner DBs. Never attach more than ~10 at once.

### Embeddings

Multimodal embeddings (Gemini Embedding 2) map text, images, audio, video into a single semantic space. Model-agnostic — stored alongside the data, not as the data. Provider change protocol: archive current positions, run parallel embedding, verify semantic neighbourhoods before transitioning.

### Why a context graph, not a vector store

Vector search returns document chunks ranked by similarity. A context graph returns structured knowledge — entities, relationships, and temporal validity.

When a new signal says a practitioner has left a scene, the graph invalidates the BELONGS_TO edge, preserves the history, and updates every query that touches that relationship. Typed edges with temporal validity give the system what embeddings alone cannot. Embeddings serve positioning and discovery. The graph serves knowledge.

### Signal flow

```
SIGNAL ARRIVES
  (contribution, scout agent, practitioner input, recorded conversation)
    |
SOURCE CLASSIFICATION + PROVENANCE
  source_origin: human_primary / human_secondary / ai_assisted / ai_generated / unknown
  event_time + ingestion_time + consent terms
    |
PROCESSING
  Entity extraction, edge type classification, processing trace logged
  Ouroboros check: is this signal derived from a previous A(DAI) output?
    |
INTAKE QUEUE (staging — "submitted, pending")
    |
MERGE BOUNDARY
  Auto-merge (practitioner self-report on own DB, trusted contributor)
  Hold for review (new contributor, external contribution about a practitioner)
  Frontier flag (can't classify → 90-day lifecycle)
  Consent terms enforced here
    |
GRAPH (CR-SQLite Matryoshka)
  Practitioner DBs ← scene DBs ← field DB
  Typed edges, full provenance, bi-temporal timestamps
    |
CONTRIBUTION RECEIPT
  Contributor sees what was extracted, what connections formed, where their signal sits
```

### Ouroboros defence

Source origin typing prevents recursive quality degradation: `human_primary` (direct practitioner contribution), `human_secondary` (human-authored article), `ai_assisted` (human-authored with AI tools), `ai_generated` (fully AI-generated), `unknown`. The merge boundary weights these differently. `ai_generated` requires review regardless of contributor trust. Cycle detection flags signals whose provenance traces back through AI processing to a previous A(DAI) output.

### Interoperability targets

- **Getty AAT/ULAN** — map concepts and practitioners to Getty identifiers
- **Wikidata** — export practitioner and concept nodes where licence-compatible
- **C2PA / Content Credentials** — integrate production provenance for artwork nodes
- **IPFS CIDs** — content-addressed identifiers for artwork media

Schema fields from launch, even if unpopulated.

### Entity IDs

Human-readable IDs with type prefix: `artwork:fidenza`, `practitioner:casey reas`, `concept:generative code`. Not kebab-case slugs or UUIDs.

---

## 07. The Merge Boundary

Every signal entering the graph passes through a reviewed gate. The parameters at this gate — what auto-merges, what gets held, what triggers frontier flagging — are curatorial decisions made executable, auditable, and revisable.

### Trust model

| Trust level | Who | Behaviour |
|---|---|---|
| New contributor | First submission | Held for review |
| Reviewed | Established track record | Auto-merge eligible |
| Practitioner (self-report) | Artist on their own work | Highest epistemic authority, no merge boundary on own DB |
| Agent (scaffolded) | Agent surfaces candidates, practitioner approves | Dual provenance |
| Agent (autonomous) | Established track record, explicit accountability | Trust-level upgrade, not the default |

### Frontier signals

When the system encounters something it can't classify, that's not an error — it's a frontier signal. These mark where digital arts practice is ahead of the system's vocabulary. Frontier signals carry a 90-day lifecycle: absorbed into the vocabulary, reclassified, or archived with a rationale. They are surfaced, not hidden.

Frontier signals carry extra gravitational mass precisely because they have few connections by definition.

### Open, versioned, forkable

Every merge parameter is a versioned document in the commons. Anyone can fork the parameters — a different institution could take the same protocol, apply different rules, and run a different commons on the same data. That is the structural guarantee against enclosure.

---

## 08. Sensing and Narrative

### What is committed

- Every interaction generates a signal
- Signals that pass the merge boundary update the graph immediately
- The narrative engine reflects the current graph state, not a cached snapshot
- Frontier signals are surfaced, not queued for a batch cycle
- Processing traces are mandatory: every signal processed through Claude carries what it extracted, rejected, and at what confidence

### What is open for design

The tidal rhythm model (6h/24h/72h pulse/survey/dream) from v3.1 is a hypothesis, not a commitment. The primary sensing rhythm is the interaction itself — every conversation is a sensing event.

Open questions:
- Whether deeper structural analysis (dream cycle, CLA consolidation, tendency vocabulary evolution) runs on a tidal rhythm, is triggered by threshold events, or operates some other way
- How external calibration works in practice (quarterly blind spot audits are a starting point)
- Whether the sensing layer is a separate process or emerges from interaction patterns

### Narrative generation (destination)

The narrative engine is a core component of the full vision. It produces situated, partial readings of the field — not summaries, but perspectives that foreground specific tensions and structural positions.

**Every narrative is a provocation.** The system does not aim for balance. It aims for honesty about its own partiality. A narrative might foreground a tension, trace an influence chain that challenges received wisdom, or surface a region where the vocabulary is still forming. It names what it's leaving out.

**Narrative provenance:** Every generated narrative carries metadata: which nodes and edges it drew from, which traversal paths it took, what it omitted and why. The narrative is auditable.

**Anti-sycophancy:** Divergence between Claude's structural analysis and visitor responses is healthy. If they converge suspiciously, the system may be manufacturing intention rather than detecting it.

### Intention over attention — metrics

The system does not track time-on-page, click-through, or audience growth. It measures:

- **Relational density** — how richly connected the graph is, weighted by edge type diversity per node
- **Edge diversity** — distribution of edge types. Prevents collapse into a single relation type
- **Provenance quality** — percentage of signals with complete provenance chains
- **Frontier signal rate** — signals that generate new concept nodes or edge types
- **Interpretive diversity (primary alarm)** — low narrative diversity signals monoculture. If multiple scout agents return structurally similar signals despite different skill files, the processing model is producing epistemic homogenisation

---

## 09. Agentic Risk Map

Mapped from "Agents of Chaos" (Shapira et al., Feb 2026) — 11 failure modes from live red-teaming of autonomous LLM agents, plus GDPR erasure.

| Risk | A(DAI) defence | Status |
|---|---|---|
| Disproportionate response | Constrained mutation operations (no raw SQL) | Principle exists, not enforced |
| Non-owner compliance | Trust tiers + merge boundary | Designed, not in schema |
| Information disclosure | Consent architecture | Not in db.sql |
| Resource looping | Rate limits + dedup | Not built |
| Denial of service | Rate limits on contribution endpoint | Not built |
| Provider value override | Editorial guidance docs | **Not written — highest-risk gap** |
| Self-harm / config tampering | Read-only pipeline config | Not enforced |
| Identity spoofing | Authentication | Not designed |
| Cross-agent corruption | Stateless pipeline + merge boundary | Needs enforcement |
| Persistent state injection | Signal content as data, not instructions | Needs enforcement |
| Libel / misinformation | Confidence levels + practitioner correction | Schema exists, UX not built |
| GDPR erasure vs "never delete" | Dedicated erasure tool across Matryoshka | Not designed |

**GDPR erasure:** A(DAI) operates under EU jurisdiction. GDPR Article 17 gives individuals the right to erasure. The "never delete, only invalidate" principle directly conflicts. Invalidation with a timestamp is not deletion. A dedicated erasure tool must propagate hard deletion across all three Matryoshka layers with audit trail. This is a legal requirement before onboarding EU-resident practitioners at scale.

**Priority before Basel:** (1) Constrain pipeline to predefined mutation ops, (2) trust tiers in contributor schema, (3) write editorial guidance docs, (4) rate limiting on contribution, (5) authentication flow.

**Priority before public scale:** (1) GDPR erasure propagation, (2) consent enforcement at query layer, (3) practitioner correction flow, (4) input sanitisation, (5) processing traces with full audit trail.

---

## 10. Governance

### The architectural backstop

Fork rights are declared from the start. The data is CC-BY-SA. The parameters are open. The protocol is open source. Anyone can take the commons, apply different merge rules, and run a different institution on the same intelligence.

### How governance develops

**Now:** The founding team makes all editorial decisions. Acknowledged, not hidden. All parameters committed to git with rationale.

**Trust-based auto-merge (launch):** New contributors start reviewed. Track record earns auto-merge. A profile field, not a committee.

**Contributor feedback (post-Basel):** Contributors flag disagreements. Flags aggregate. At threshold, they trigger review with a public response.

**Formal contestation (50+ contributors):** Documented challenge process. Rotating review group including contributors from outside the founding team.

**Governance transition trigger:** 200 active contributors OR 2 years, whichever comes first. Review is binding. Founding team authority structurally constrained. Contributors vote on processing rules, not individual signals.

### Growth model: orbital accretion

A(DAI) does not launch — it accretes. Mass forms at centre first.

| Orbit | Size | Who | Threshold to open next |
|---|---|---|---|
| 0 — Core | ~10 | Founding team | Prototype live, graph queryable |
| 1 — First light | ~50–100 | Pioneering practitioners, founding partners | 20+ practitioner contributions processed, narrative generation working |
| 2 — Inner ring | ~500 | Broader practitioner community | Interpretive diversity healthy, 3+ scenes forming |
| 3 — Outer ring | ~5,000 | Curators, researchers, emerging practitioners | Federation protocol live, governance transition review triggered |
| 4 — Open field | Unlimited | General public | Gravitational centres robust enough to absorb anonymous contribution |

---

## 11. Skills Files

Skills files are not documentation — they are context that must be read and passed in Claude API calls. If they're not injected, they do nothing.

```
/skills/
  tendency-vocabulary.md    # The evaluative vocabulary
  cla-extraction.md         # CLA framework for signal processing
  scout-editorial.md        # Scout agent editorial brief
```

**Status:** None written yet. This is the highest-priority gap for the processing pipeline.

### Core rules

1. **Judgment in skills, not Python.** If code is making linking decisions via string matching, that's judgment in the wrong place.
2. **Skills must be injected.** Read and pass as context in every Claude API call.
3. **Context before execution.** Every pipeline run should know its own state.
4. **Frontier signals are the point.** Every signal exits with explicit status: `processed` | `concept_linked` | `frontier` | `error`.
5. **Processing traces are mandatory.** Every signal carries what was extracted, rejected, and at what confidence.
6. **Ouroboros defence.** Source origin typing. Raw practitioner voice is the highest-value signal.

### Tendency axes

Evaluative dimensions, not categories:
- openness ↔ enclosure
- commons ↔ capture
- spectacle ↔ infrastructure
- individual ↔ collective
- attention ↔ intention

---

## 12. Current State (April 2026)

### What's live

CR-SQLite backend (Gio, Shards) deployed at [adai-basel.fly.dev](https://adai-basel.fly.dev). Single-DB deployment.

| Component | Status |
|---|---|
| CR-SQLite schema (4 CRR tables, composite PK on edges) | ✅ Live |
| Shards HTTP server (all endpoints) | ✅ Live |
| Fly.io deployment (Docker) | ✅ Live |
| Seed data pipeline (contract-based gatherers → 4,509 nodes / 17,385 edges, two-platform May 2026 rebuild) | ✅ Done |
| Contribution flow (`/contribute` → intake_queue → `/review`) | ✅ Working |
| Practitioner profiles (`/practitioner/:slug`) | ✅ Working |
| Data export (`/practitioner/:slug/data` as JSON) | ✅ Working |

### What's in progress

| Who | Focus | Status |
|---|---|---|
| **Iri** | North Star article for Substack | Draft, needs rhythm pass |
| **Iri** | 10–15 signal sources for public layer | Identifying this week |
| **Iri** | Value-based framework for scout agents | Starting |
| **Piyush** | Particle/gravitational visualisations + brand system | In progress, aesthetic approved |
| **Piyush** | Generative landing page → Netlify | Building |
| **Gio** | Merge `feat/cr-sqlite-backend` to main | Before April 9 |
| **Gio** | Claude skill file for repo | Next |
| **Gio** | Public layer ingestion experiment | April 9+ (Sicily sprint) |
| **JB** | Sourcing archivists and early practitioners | Ongoing |
| **JB** | LLM interview concept | Under evaluation |

### What needs building next

**Backend (Gio, April 9–20):**
1. Merge branch to main — make the backend canonical
2. Create Claude skill for repo — so the team can query the DB through Claude
3. Scout agent pipeline — take Iri's sources, build ingestion flow (see Layer 2 above)
4. Schema additions: trust tiers on contributor table, consent fields on signals, processing traces, bi-temporal `event_time` on edges
5. Constrain mutation operations — predefined functions only, no raw SQL (top agentic risk mitigation)
6. Basic rate limiting on `/contribute`

**Editorial (Iri):**
1. Curated source list with rationale for each source
2. Scout editorial skill file — the values and lens agents carry
3. Tendency vocabulary skill file
4. CLA extraction skill file

**Frontend (Piyush):**
1. Finish brand system (logos, fonts, colours, generative collateral)
2. Deploy landing page on Netlify
3. Three design explorations: light field, colour-as-interaction, scroll-to-zoom
4. Connection to live backend starts when Gio is available (April 9+)

**Do NOT build yet:**
- Matryoshka per-practitioner split — not a blocker, single DB is fine
- Embedding pipeline — post-foundation
- Audio/voice anything — post-foundation
- CRDT sync between instances — not needed until forks exist
- Authentication — needed before Basel, not this sprint

### Critical gaps

1. **Artwork image coverage.** 399 artwork nodes are now in the graph (MoMA CSV + Wikidata SPARQL + Art Blocks Hasura + fxhash GraphQL, April 21–22 enrichment). Wikidata image patches covered 32% of practitioners but 0% of artworks; image sourcing for artworks remains a follow-up pass.
2. **Cross-source dedup not yet verified.** Net-new artworks from MoMA (~89 KB) and fxhash (~9.4 KB) are staged in `seed/_build/*_new_artworks.json` — merge into `nodes.json` is not yet verified. The Met OpenAccess fetch returned empty.
3. **Editorial guidance docs unwritten.** This is the highest-risk gap. Without skills files, Claude classifies with its own defaults — safe, generic, Western-institutional. The editorial lens that makes A(DAI) different from a search engine lives in these documents. They must be authored and injected in every API call.
4. **No authentication system.** Critical if practitioners are onboarded live at Basel. Doesn't need to be complex — invite-link tokens would work to start.
5. **Geographic intake bias.** 47 of 59 practitioners are North American/European. The public layer should intentionally diversify through source selection.
6. **Consent fields not in db.sql.** `consent_scope`, `consent_attribution`, `consent_revocable` needed on signals table before onboarding practitioners who share under restricted terms.

---

## 13. Quality Tests

If any of these are true, the system is failing:
- No frontier signals visible → merge boundary too tight
- Every auto-merge confirms the founding team's map → system mirrors its founders
- The narrative sounds the same every session → the generative interface isn't generative
- Graph collapses to a handful of edge types → semantic layer is cosmetic
- Artworks exist but have no edges → illustrations, not first-class nodes
- Nobody asks for a different perspective → the system isn't provoking curiosity
- Visitors can't tell what the system left out → the partiality isn't honest
- No contribution receipt → system takes without showing what it did
- People listen but never speak → interaction model isn't inviting challenge
- Challenges always get agreeable responses → anti-sycophancy has failed

---

## 14. Document Hierarchy

1. **This seed document (v4.1)** — the living specification. Architecture, principles, typed edges, merge boundary, gravity model, trust model, gap table.
2. **CLAUDE.md** — the authoritative project reference for development. Updated April 2026. Synthesises all current decisions.
3. **North Star** (Notion) — the public-facing articulation for Substack.
4. **Design Brief: How the Backend Shapes the Frontend** (Notion) — what the backend means for design, what can be built now vs later.
5. **Agentic Risk Map** (Notion) — 12 failure modes mapped to A(DAI) with priority actions.
6. **adaiconceptpaper.pdf** — Iri's foundational vision. Authoritative on thesis and positioning.
7. **adai_living_graph_spec.pdf** — JB's living graph spec. Audio interaction model = current reference. Architecture = historical.
8. **Backend & Storage Idea** (Notion) — Gio's CR-SQLite Matryoshka proposal.

---

## 15. Stack

CR-SQLite (Gio's C port), Shards (HTTP server + scripting), Python (pipeline scripts), Anthropic API (Claude — processing, classification, narrative generation, query), Gemini Embedding 2 (embeddings — future), Fly.io (backend — adai-basel.fly.dev), Netlify (landing page — Piyush), Docker (deployment), D3 (graph visualisation — starting point), TouchDesigner (particle/gravitational viz — Piyush). TTS and transcription providers TBD (post-foundation).

---

## 16. Team

- **Iri** — strategy, editorial, signal source curation, value framework for agents, build coordination
- **JB** — market development, artist relations, sensing conversations, LLM interview concept
- **Gio** — backend architecture, CR-SQLite, protocol, public layer data collection
- **Piyush** — frontend, generative landing page, brand system, particle/gravitational visualisation

---

## Changelog

### v4.1 — 2026-04-03

**Clarity pass.** Rewrote Section 04 (Three-Layer Prototype) with explicit what/why/why-this-order for each layer, matching the tone of the Team Brief. Added "Why layers, not rooms" rationale. Added source priority table and scout agent pipeline steps to Layer 2. Rewrote Section 12 (Current State) with architecture status table, structured action items by person, and explicit "do NOT build yet" list. Added "single DB first, Matryoshka later" decision statement to Section 06.

### v4.0 — 2026-04-03

**Strategic pivot:** Four-room Basel model replaced by three-layer prototype (generative landing page + public data layer + intimate layer). The rooms were an interaction model; the layers are a build sequence. Plants grow from roots.

**Mode 2 dropped.** There is no advisory services layer. The commons is the product. Period.

**Two data layers formalised.** Public (crawled from open sources, agent-classified, confidence: medium) and Intimate (practitioner input, lived experience, confidence: high). Both flow through the same backend. Gio named the visual metaphor: Dante's inferno underneath, refined surface on top.

**Audio layer post-foundation.** Voice input, TTS, pause-and-challenge — still the vision, but only built once data layers, protocol, and graph density are solid.

**Agentic risk map.** 12 risks from "Agents of Chaos" paper mapped to A(DAI) architecture. GDPR erasure identified as legal requirement conflicting with "never delete" principle.

**LLM interviews under evaluation.** JB proposed LLM-conducted interviews. Critical caveat from Iri: needs human-in-the-loop or it's a glorified chatbot. Not confirmed.

**Aesthetic direction.** Piyush building particle/gravitational visualisations. Design explicitly connected to North Star principles.

**Sensing loop.** Tidal rhythms (6h/24h/72h) moved from commitment to hypothesis. Primary sensing rhythm is the interaction itself.

**Workstreams.** 12-week parallel workstream timeline removed. Replaced by three-layer prototype sequencing.

**Basel.** No longer scoped as four rooms. Basel is where we show the product if timing aligns.

### v3.1 — 2026-03-25
Field research integration. Basel scope revision (5→4 rooms). Agent scaffolding. Processing traces. External calibration. Anticipatory sensing. Interoperability targets. Consent policy. Mode 2 use cases.

### v3.0 — 2026-03-24
Combined Design Brief v2.2 and Garden Logic v1.0 into single source of truth.

### v2.2 — 2026-03-20
Agent-native backend and practitioner agents.

### v2.1 — 2026-03-19
Artworks as first-class nodes.

### v2.0 — 2026-03-19
Conversations as primary signal. Sense room as centrepiece. Governance architecture. CC-BY-SA.

### v1.0 — 2026-03-19
Initial versioned brief.
